# Local workspace the plugin proposes for mapper output / generated scripts
sz-workspace/
*_output.jsonl

# Local Claude Code state (settings.local.json, session scratch) — not published
.claude/

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/
